{
    "files": [{
        "from": "view.coffee.hbs",
        "to": "app/views/{{name}}.coffee"
    }],
    "dependencies": [{
        "name": "model",
        "params": "{{name}}"
    }, {
        "name": "view_spec",
        "params": "{{name}}"
    }, {
        "name": "style",
        "params": "{{name}}"
    }, {
        "name": "template",
        "params": "{{name}}"
    }]
}
